/** @type {import('tailwindcss').Config} */ 
module.exports = {
    content: ["./src/**/*.{html,js}"],
    theme: {
      extend: {},
    },
    plugins: [],
  }




 // we need this file for our every tailwind project
 //And we can find this from tailwind doc..file name will be tailwind.config.js..we just need to copy the file and paste it here with the same name